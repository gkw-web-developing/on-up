generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model products {
  id    Int    @id @default(autoincrement())
  title String @default("Książka") @db.VarChar(256)
}
